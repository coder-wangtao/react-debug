1.初始渲染

2. 状态与 Props 变更

3.Context 变化

4.Actions(React 19)

5.use(Promise)中/Promise resolve 时触发更新

5.  import { flushSync } from 'react-dom';
    // flushSync：强制同步执行更新

创建 Update 对象 -> 计算优先级（Lane）-> 将 Update 入队。
